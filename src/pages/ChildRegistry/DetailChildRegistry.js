import React from 'react';

import '../../styles/ChildRegistry/detail-child-registry.css'

export default function DetailChildRegistry() {
  return (
    // rota da api: GET /child/:id
    <h1>See child registry</h1>
  );
}